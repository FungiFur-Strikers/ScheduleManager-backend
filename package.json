{
  "name": "ScheduleManager-backend",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "lint": "biome lint .",
    "format": "biome format . --write",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "@biomejs/biome": "^1.9.4",
    "hono": "^4.7.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "wrangler": "4.2.0"
  }
}